name: gog
description: Check Gmail and Google Workspace
author: albertslee
version: 0.1.0

entrypoint: gog.py

inputs:
  query:
    type: string
    description: What to check

outputs:
  result:
    type: string
